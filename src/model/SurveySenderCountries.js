/**
 * Textmagic API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SurveySenderCountries model module.
 * @module model/SurveySenderCountries
 * @version 3.0.43874
 */
class SurveySenderCountries {
    /**
     * Constructs a new <code>SurveySenderCountries</code>.
     * @alias module:model/SurveySenderCountries
     * @param id {Number} 
     * @param countryName {String} 
     * @param fromNumber {String} 
     * @param allowDedicated {Boolean} Is allowed to use a dedicated number?
     */
    constructor(id, countryName, fromNumber, allowDedicated) { 
        
        SurveySenderCountries.initialize(this, id, countryName, fromNumber, allowDedicated);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, countryName, fromNumber, allowDedicated) { 
        obj['id'] = id;
        obj['countryName'] = countryName;
        obj['fromNumber'] = fromNumber;
        obj['allowDedicated'] = allowDedicated;
    }

    /**
     * Constructs a <code>SurveySenderCountries</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SurveySenderCountries} obj Optional instance to populate.
     * @return {module:model/SurveySenderCountries} The populated <code>SurveySenderCountries</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SurveySenderCountries();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('countryName')) {
                obj['countryName'] = ApiClient.convertToType(data['countryName'], 'String');
            }
            if (data.hasOwnProperty('fromNumber')) {
                obj['fromNumber'] = ApiClient.convertToType(data['fromNumber'], 'String');
            }
            if (data.hasOwnProperty('allowDedicated')) {
                obj['allowDedicated'] = ApiClient.convertToType(data['allowDedicated'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SurveySenderCountries</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SurveySenderCountries</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SurveySenderCountries.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['countryName'] && !(typeof data['countryName'] === 'string' || data['countryName'] instanceof String)) {
            throw new Error("Expected the field `countryName` to be a primitive type in the JSON string but got " + data['countryName']);
        }
        // ensure the json data is a string
        if (data['fromNumber'] && !(typeof data['fromNumber'] === 'string' || data['fromNumber'] instanceof String)) {
            throw new Error("Expected the field `fromNumber` to be a primitive type in the JSON string but got " + data['fromNumber']);
        }

        return true;
    }


}

SurveySenderCountries.RequiredProperties = ["id", "countryName", "fromNumber", "allowDedicated"];

/**
 * @member {Number} id
 */
SurveySenderCountries.prototype['id'] = undefined;

/**
 * @member {String} countryName
 */
SurveySenderCountries.prototype['countryName'] = undefined;

/**
 * @member {String} fromNumber
 */
SurveySenderCountries.prototype['fromNumber'] = undefined;

/**
 * Is allowed to use a dedicated number?
 * @member {Boolean} allowDedicated
 */
SurveySenderCountries.prototype['allowDedicated'] = undefined;






export default SurveySenderCountries;

