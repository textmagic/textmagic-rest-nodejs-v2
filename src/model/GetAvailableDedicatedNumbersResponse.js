/**
 * Textmagic API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GetAvailableDedicatedNumbersResponse model module.
 * @module model/GetAvailableDedicatedNumbersResponse
 * @version 3.0.43874
 */
class GetAvailableDedicatedNumbersResponse {
    /**
     * Constructs a new <code>GetAvailableDedicatedNumbersResponse</code>.
     * @alias module:model/GetAvailableDedicatedNumbersResponse
     * @param numbers {Array.<String>} Array of phone numbers.
     * @param price {Number} Dedicated number monthly fee for this country. Returned in the current [account](https://docs.textmagic.com/#tag/User) currency.
     * @param giftType {module:model/GetAvailableDedicatedNumbersResponse.GiftTypeEnum} 
     */
    constructor(numbers, price, giftType) { 
        
        GetAvailableDedicatedNumbersResponse.initialize(this, numbers, price, giftType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, numbers, price, giftType) { 
        obj['numbers'] = numbers;
        obj['price'] = price;
        obj['giftType'] = giftType;
    }

    /**
     * Constructs a <code>GetAvailableDedicatedNumbersResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetAvailableDedicatedNumbersResponse} obj Optional instance to populate.
     * @return {module:model/GetAvailableDedicatedNumbersResponse} The populated <code>GetAvailableDedicatedNumbersResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetAvailableDedicatedNumbersResponse();

            if (data.hasOwnProperty('numbers')) {
                obj['numbers'] = ApiClient.convertToType(data['numbers'], ['String']);
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'Number');
            }
            if (data.hasOwnProperty('giftType')) {
                obj['giftType'] = ApiClient.convertToType(data['giftType'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetAvailableDedicatedNumbersResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetAvailableDedicatedNumbersResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetAvailableDedicatedNumbersResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['numbers'])) {
            throw new Error("Expected the field `numbers` to be an array in the JSON data but got " + data['numbers']);
        }
        // ensure the json data is a string
        if (data['giftType'] && !(typeof data['giftType'] === 'string' || data['giftType'] instanceof String)) {
            throw new Error("Expected the field `giftType` to be a primitive type in the JSON string but got " + data['giftType']);
        }

        return true;
    }


}

GetAvailableDedicatedNumbersResponse.RequiredProperties = ["numbers", "price", "giftType"];

/**
 * Array of phone numbers.
 * @member {Array.<String>} numbers
 */
GetAvailableDedicatedNumbersResponse.prototype['numbers'] = undefined;

/**
 * Dedicated number monthly fee for this country. Returned in the current [account](https://docs.textmagic.com/#tag/User) currency.
 * @member {Number} price
 */
GetAvailableDedicatedNumbersResponse.prototype['price'] = undefined;

/**
 * @member {module:model/GetAvailableDedicatedNumbersResponse.GiftTypeEnum} giftType
 */
GetAvailableDedicatedNumbersResponse.prototype['giftType'] = undefined;





/**
 * Allowed values for the <code>giftType</code> property.
 * @enum {String}
 * @readonly
 */
GetAvailableDedicatedNumbersResponse['GiftTypeEnum'] = {

    /**
     * value: "none"
     * @const
     */
    "none": "none",

    /**
     * value: "registration"
     * @const
     */
    "registration": "registration",

    /**
     * value: "first_month"
     * @const
     */
    "first_month": "first_month",

    /**
     * value: "include_in_plan"
     * @const
     */
    "include_in_plan": "include_in_plan"
};



export default GetAvailableDedicatedNumbersResponse;

