/**
 * Textmagic API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MessageSession model module.
 * @module model/MessageSession
 * @version 3.0.43879
 */
class MessageSession {
    /**
     * Constructs a new <code>MessageSession</code>.
     * @alias module:model/MessageSession
     * @param id {Number} Session ID.
     * @param startTime {String} Session creation time.
     * @param text {String} Session text. If a template was used for the session text (see [Messages: Send](https://docs.textmagic.com/#tag/Outbound-Messages) for details), it may contain template tags. 
     * @param source {String} *   **O** – for Textmagic Online; *   **A** – for API; *   **M** – for Textmagic Messenger; *   **E** – for [Email to SMS](https://docs.textmagic.com/#tag/Send-Email-to-SMS); *   **X** – for [Distribution Lists](https://docs.textmagic.com/#tag/Distribution-Lists). 
     * @param referenceId {String} Custom reference ID (see [Messages: Send](https://docs.textmagic.com/#tag/Send-Email-to-SMS) for details). 
     * @param price {Number} Session cost (in account currency).
     * @param numbersCount {Number} Session recipient count.
     * @param destination {module:model/MessageSession.DestinationEnum} Destination type of a Message Session: * **t** – text SMS; * **s** – text-to-speech; * **v** – voice broadcast. 
     * @param initiatorId {Number} Initiator ID.
     * @param title {String} 
     */
    constructor(id, startTime, text, source, referenceId, price, numbersCount, destination, initiatorId, title) { 
        
        MessageSession.initialize(this, id, startTime, text, source, referenceId, price, numbersCount, destination, initiatorId, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, startTime, text, source, referenceId, price, numbersCount, destination, initiatorId, title) { 
        obj['id'] = id;
        obj['startTime'] = startTime;
        obj['text'] = text;
        obj['source'] = source;
        obj['referenceId'] = referenceId;
        obj['price'] = price;
        obj['numbersCount'] = numbersCount;
        obj['destination'] = destination;
        obj['initiatorId'] = initiatorId;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>MessageSession</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MessageSession} obj Optional instance to populate.
     * @return {module:model/MessageSession} The populated <code>MessageSession</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MessageSession();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('startTime')) {
                obj['startTime'] = ApiClient.convertToType(data['startTime'], 'String');
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
            if (data.hasOwnProperty('referenceId')) {
                obj['referenceId'] = ApiClient.convertToType(data['referenceId'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'Number');
            }
            if (data.hasOwnProperty('numbersCount')) {
                obj['numbersCount'] = ApiClient.convertToType(data['numbersCount'], 'Number');
            }
            if (data.hasOwnProperty('destination')) {
                obj['destination'] = ApiClient.convertToType(data['destination'], 'String');
            }
            if (data.hasOwnProperty('initiatorId')) {
                obj['initiatorId'] = ApiClient.convertToType(data['initiatorId'], 'Number');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MessageSession</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MessageSession</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MessageSession.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['startTime'] && !(typeof data['startTime'] === 'string' || data['startTime'] instanceof String)) {
            throw new Error("Expected the field `startTime` to be a primitive type in the JSON string but got " + data['startTime']);
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }
        // ensure the json data is a string
        if (data['referenceId'] && !(typeof data['referenceId'] === 'string' || data['referenceId'] instanceof String)) {
            throw new Error("Expected the field `referenceId` to be a primitive type in the JSON string but got " + data['referenceId']);
        }
        // ensure the json data is a string
        if (data['destination'] && !(typeof data['destination'] === 'string' || data['destination'] instanceof String)) {
            throw new Error("Expected the field `destination` to be a primitive type in the JSON string but got " + data['destination']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

MessageSession.RequiredProperties = ["id", "startTime", "text", "source", "referenceId", "price", "numbersCount", "destination", "initiatorId", "title"];

/**
 * Session ID.
 * @member {Number} id
 */
MessageSession.prototype['id'] = undefined;

/**
 * Session creation time.
 * @member {String} startTime
 */
MessageSession.prototype['startTime'] = undefined;

/**
 * Session text. If a template was used for the session text (see [Messages: Send](https://docs.textmagic.com/#tag/Outbound-Messages) for details), it may contain template tags. 
 * @member {String} text
 */
MessageSession.prototype['text'] = undefined;

/**
 * *   **O** – for Textmagic Online; *   **A** – for API; *   **M** – for Textmagic Messenger; *   **E** – for [Email to SMS](https://docs.textmagic.com/#tag/Send-Email-to-SMS); *   **X** – for [Distribution Lists](https://docs.textmagic.com/#tag/Distribution-Lists). 
 * @member {String} source
 */
MessageSession.prototype['source'] = undefined;

/**
 * Custom reference ID (see [Messages: Send](https://docs.textmagic.com/#tag/Send-Email-to-SMS) for details). 
 * @member {String} referenceId
 */
MessageSession.prototype['referenceId'] = undefined;

/**
 * Session cost (in account currency).
 * @member {Number} price
 */
MessageSession.prototype['price'] = undefined;

/**
 * Session recipient count.
 * @member {Number} numbersCount
 */
MessageSession.prototype['numbersCount'] = undefined;

/**
 * Destination type of a Message Session: * **t** – text SMS; * **s** – text-to-speech; * **v** – voice broadcast. 
 * @member {module:model/MessageSession.DestinationEnum} destination
 */
MessageSession.prototype['destination'] = undefined;

/**
 * Initiator ID.
 * @member {Number} initiatorId
 */
MessageSession.prototype['initiatorId'] = undefined;

/**
 * @member {String} title
 */
MessageSession.prototype['title'] = undefined;





/**
 * Allowed values for the <code>destination</code> property.
 * @enum {String}
 * @readonly
 */
MessageSession['DestinationEnum'] = {

    /**
     * value: "t"
     * @const
     */
    "t": "t",

    /**
     * value: "s"
     * @const
     */
    "s": "s",

    /**
     * value: "v"
     * @const
     */
    "v": "v"
};



export default MessageSession;

