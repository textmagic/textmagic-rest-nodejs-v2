/**
 * Textmagic API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The FavoriteContact model module.
 * @module model/FavoriteContact
 * @version 3.0.43879
 */
class FavoriteContact {
    /**
     * Constructs a new <code>FavoriteContact</code>.
     * @alias module:model/FavoriteContact
     * @extends Object
     * @param entityId {Number} List or Contact ID.
     * @param entityType {module:model/FavoriteContact.EntityTypeEnum} Entity type which should be marked as **favorite**.
     * @param primaryLabel {String} Contact first name/last name if entityType is **contact**; List name if entity type is **list**.
     * @param secondaryLabel {String} Phone number if entityType is **contact**; List contacts number if entity type is **list**.
     * @param tertiaryLabel {String} Contact country if entityType is **contact**; else, null.
     * @param avatar {String} 
     */
    constructor(entityId, entityType, primaryLabel, secondaryLabel, tertiaryLabel, avatar) { 
        
        FavoriteContact.initialize(this, entityId, entityType, primaryLabel, secondaryLabel, tertiaryLabel, avatar);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, entityId, entityType, primaryLabel, secondaryLabel, tertiaryLabel, avatar) { 
        obj['entityId'] = entityId;
        obj['entityType'] = entityType;
        obj['primaryLabel'] = primaryLabel;
        obj['secondaryLabel'] = secondaryLabel;
        obj['tertiaryLabel'] = tertiaryLabel;
        obj['avatar'] = avatar;
    }

    /**
     * Constructs a <code>FavoriteContact</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FavoriteContact} obj Optional instance to populate.
     * @return {module:model/FavoriteContact} The populated <code>FavoriteContact</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FavoriteContact();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('entityId')) {
                obj['entityId'] = ApiClient.convertToType(data['entityId'], 'Number');
            }
            if (data.hasOwnProperty('entityType')) {
                obj['entityType'] = ApiClient.convertToType(data['entityType'], 'String');
            }
            if (data.hasOwnProperty('primaryLabel')) {
                obj['primaryLabel'] = ApiClient.convertToType(data['primaryLabel'], 'String');
            }
            if (data.hasOwnProperty('secondaryLabel')) {
                obj['secondaryLabel'] = ApiClient.convertToType(data['secondaryLabel'], 'String');
            }
            if (data.hasOwnProperty('tertiaryLabel')) {
                obj['tertiaryLabel'] = ApiClient.convertToType(data['tertiaryLabel'], 'String');
            }
            if (data.hasOwnProperty('avatar')) {
                obj['avatar'] = ApiClient.convertToType(data['avatar'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FavoriteContact</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FavoriteContact</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FavoriteContact.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['entityType'] && !(typeof data['entityType'] === 'string' || data['entityType'] instanceof String)) {
            throw new Error("Expected the field `entityType` to be a primitive type in the JSON string but got " + data['entityType']);
        }
        // ensure the json data is a string
        if (data['primaryLabel'] && !(typeof data['primaryLabel'] === 'string' || data['primaryLabel'] instanceof String)) {
            throw new Error("Expected the field `primaryLabel` to be a primitive type in the JSON string but got " + data['primaryLabel']);
        }
        // ensure the json data is a string
        if (data['secondaryLabel'] && !(typeof data['secondaryLabel'] === 'string' || data['secondaryLabel'] instanceof String)) {
            throw new Error("Expected the field `secondaryLabel` to be a primitive type in the JSON string but got " + data['secondaryLabel']);
        }
        // ensure the json data is a string
        if (data['tertiaryLabel'] && !(typeof data['tertiaryLabel'] === 'string' || data['tertiaryLabel'] instanceof String)) {
            throw new Error("Expected the field `tertiaryLabel` to be a primitive type in the JSON string but got " + data['tertiaryLabel']);
        }
        // ensure the json data is a string
        if (data['avatar'] && !(typeof data['avatar'] === 'string' || data['avatar'] instanceof String)) {
            throw new Error("Expected the field `avatar` to be a primitive type in the JSON string but got " + data['avatar']);
        }

        return true;
    }


}

FavoriteContact.RequiredProperties = ["entityId", "entityType", "primaryLabel", "secondaryLabel", "tertiaryLabel", "avatar"];

/**
 * List or Contact ID.
 * @member {Number} entityId
 */
FavoriteContact.prototype['entityId'] = undefined;

/**
 * Entity type which should be marked as **favorite**.
 * @member {module:model/FavoriteContact.EntityTypeEnum} entityType
 */
FavoriteContact.prototype['entityType'] = undefined;

/**
 * Contact first name/last name if entityType is **contact**; List name if entity type is **list**.
 * @member {String} primaryLabel
 */
FavoriteContact.prototype['primaryLabel'] = undefined;

/**
 * Phone number if entityType is **contact**; List contacts number if entity type is **list**.
 * @member {String} secondaryLabel
 */
FavoriteContact.prototype['secondaryLabel'] = undefined;

/**
 * Contact country if entityType is **contact**; else, null.
 * @member {String} tertiaryLabel
 */
FavoriteContact.prototype['tertiaryLabel'] = undefined;

/**
 * @member {String} avatar
 */
FavoriteContact.prototype['avatar'] = undefined;





/**
 * Allowed values for the <code>entityType</code> property.
 * @enum {String}
 * @readonly
 */
FavoriteContact['EntityTypeEnum'] = {

    /**
     * value: "list"
     * @const
     */
    "list": "list",

    /**
     * value: "contact"
     * @const
     */
    "contact": "contact"
};



export default FavoriteContact;

