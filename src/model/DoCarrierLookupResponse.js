/**
 * Textmagic API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Country from './Country';

/**
 * The DoCarrierLookupResponse model module.
 * @module model/DoCarrierLookupResponse
 * @version 3.0.43874
 */
class DoCarrierLookupResponse {
    /**
     * Constructs a new <code>DoCarrierLookupResponse</code>.
     * @alias module:model/DoCarrierLookupResponse
     * @param cost {Number} The cost to check that one number is constant – 0.04 in your account currency.
     * @param local {String} Phone number in [National format](https://en.wikipedia.org/wiki/National_conventions_for_writing_telephone_numbers).
     * @param type {module:model/DoCarrierLookupResponse.TypeEnum} Phone number type.
     * @param carrier {String} Carrier name.
     * @param number164 {String} Phone number in [E.164 format](https://en.wikipedia.org/wiki/E.164).
     * @param valid {Boolean} This field shows whether the entered phone number is valid or not.
     */
    constructor(cost, local, type, carrier, number164, valid) { 
        
        DoCarrierLookupResponse.initialize(this, cost, local, type, carrier, number164, valid);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, cost, local, type, carrier, number164, valid) { 
        obj['cost'] = cost;
        obj['local'] = local;
        obj['type'] = type;
        obj['carrier'] = carrier;
        obj['number164'] = number164;
        obj['valid'] = valid;
    }

    /**
     * Constructs a <code>DoCarrierLookupResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DoCarrierLookupResponse} obj Optional instance to populate.
     * @return {module:model/DoCarrierLookupResponse} The populated <code>DoCarrierLookupResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DoCarrierLookupResponse();

            if (data.hasOwnProperty('cost')) {
                obj['cost'] = ApiClient.convertToType(data['cost'], 'Number');
            }
            if (data.hasOwnProperty('country')) {
                obj['country'] = Country.constructFromObject(data['country']);
            }
            if (data.hasOwnProperty('local')) {
                obj['local'] = ApiClient.convertToType(data['local'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('carrier')) {
                obj['carrier'] = ApiClient.convertToType(data['carrier'], 'String');
            }
            if (data.hasOwnProperty('number164')) {
                obj['number164'] = ApiClient.convertToType(data['number164'], 'String');
            }
            if (data.hasOwnProperty('valid')) {
                obj['valid'] = ApiClient.convertToType(data['valid'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DoCarrierLookupResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DoCarrierLookupResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DoCarrierLookupResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `country`
        if (data['country']) { // data not null
          Country.validateJSON(data['country']);
        }
        // ensure the json data is a string
        if (data['local'] && !(typeof data['local'] === 'string' || data['local'] instanceof String)) {
            throw new Error("Expected the field `local` to be a primitive type in the JSON string but got " + data['local']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['carrier'] && !(typeof data['carrier'] === 'string' || data['carrier'] instanceof String)) {
            throw new Error("Expected the field `carrier` to be a primitive type in the JSON string but got " + data['carrier']);
        }
        // ensure the json data is a string
        if (data['number164'] && !(typeof data['number164'] === 'string' || data['number164'] instanceof String)) {
            throw new Error("Expected the field `number164` to be a primitive type in the JSON string but got " + data['number164']);
        }

        return true;
    }


}

DoCarrierLookupResponse.RequiredProperties = ["cost", "local", "type", "carrier", "number164", "valid"];

/**
 * The cost to check that one number is constant – 0.04 in your account currency.
 * @member {Number} cost
 */
DoCarrierLookupResponse.prototype['cost'] = undefined;

/**
 * @member {module:model/Country} country
 */
DoCarrierLookupResponse.prototype['country'] = undefined;

/**
 * Phone number in [National format](https://en.wikipedia.org/wiki/National_conventions_for_writing_telephone_numbers).
 * @member {String} local
 */
DoCarrierLookupResponse.prototype['local'] = undefined;

/**
 * Phone number type.
 * @member {module:model/DoCarrierLookupResponse.TypeEnum} type
 */
DoCarrierLookupResponse.prototype['type'] = undefined;

/**
 * Carrier name.
 * @member {String} carrier
 */
DoCarrierLookupResponse.prototype['carrier'] = undefined;

/**
 * Phone number in [E.164 format](https://en.wikipedia.org/wiki/E.164).
 * @member {String} number164
 */
DoCarrierLookupResponse.prototype['number164'] = undefined;

/**
 * This field shows whether the entered phone number is valid or not.
 * @member {Boolean} valid
 */
DoCarrierLookupResponse.prototype['valid'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
DoCarrierLookupResponse['TypeEnum'] = {

    /**
     * value: "mobile"
     * @const
     */
    "mobile": "mobile",

    /**
     * value: "landline"
     * @const
     */
    "landline": "landline",

    /**
     * value: "voip"
     * @const
     */
    "voip": "voip"
};



export default DoCarrierLookupResponse;

