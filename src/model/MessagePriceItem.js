/**
 * Textmagic API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MessagePriceItem model module.
 * @module model/MessagePriceItem
 * @version 3.0.43874
 */
class MessagePriceItem {
    /**
     * Constructs a new <code>MessagePriceItem</code>.
     * @alias module:model/MessagePriceItem
     * @param name {String} Country name.
     * @param price {String} Price to send message to desired country.
     * @param country {String} The 2-letter ISO country code of the recipient's phone number.
     */
    constructor(name, price, country) { 
        
        MessagePriceItem.initialize(this, name, price, country);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, price, country) { 
        obj['name'] = name;
        obj['price'] = price;
        obj['country'] = country;
    }

    /**
     * Constructs a <code>MessagePriceItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MessagePriceItem} obj Optional instance to populate.
     * @return {module:model/MessagePriceItem} The populated <code>MessagePriceItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MessagePriceItem();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'String');
            }
            if (data.hasOwnProperty('country')) {
                obj['country'] = ApiClient.convertToType(data['country'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MessagePriceItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MessagePriceItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MessagePriceItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['price'] && !(typeof data['price'] === 'string' || data['price'] instanceof String)) {
            throw new Error("Expected the field `price` to be a primitive type in the JSON string but got " + data['price']);
        }
        // ensure the json data is a string
        if (data['country'] && !(typeof data['country'] === 'string' || data['country'] instanceof String)) {
            throw new Error("Expected the field `country` to be a primitive type in the JSON string but got " + data['country']);
        }

        return true;
    }


}

MessagePriceItem.RequiredProperties = ["name", "price", "country"];

/**
 * Country name.
 * @member {String} name
 */
MessagePriceItem.prototype['name'] = undefined;

/**
 * Price to send message to desired country.
 * @member {String} price
 */
MessagePriceItem.prototype['price'] = undefined;

/**
 * The 2-letter ISO country code of the recipient's phone number.
 * @member {String} country
 */
MessagePriceItem.prototype['country'] = undefined;






export default MessagePriceItem;

