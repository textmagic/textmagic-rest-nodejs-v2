/**
 * Textmagic API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DoAuthResponseMinVersions from './DoAuthResponseMinVersions';

/**
 * The DoAuthResponse model module.
 * @module model/DoAuthResponse
 * @version 3.0.43879
 */
class DoAuthResponse {
    /**
     * Constructs a new <code>DoAuthResponse</code>.
     * @alias module:model/DoAuthResponse
     * @param username {String} 
     * @param token {String} 
     * @param expires {Date} 
     * @param minVersions {module:model/DoAuthResponseMinVersions} 
     * @param disallowedRules {Array.<String>} 
     */
    constructor(username, token, expires, minVersions, disallowedRules) { 
        
        DoAuthResponse.initialize(this, username, token, expires, minVersions, disallowedRules);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, username, token, expires, minVersions, disallowedRules) { 
        obj['username'] = username;
        obj['token'] = token;
        obj['expires'] = expires;
        obj['minVersions'] = minVersions;
        obj['disallowedRules'] = disallowedRules;
    }

    /**
     * Constructs a <code>DoAuthResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DoAuthResponse} obj Optional instance to populate.
     * @return {module:model/DoAuthResponse} The populated <code>DoAuthResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DoAuthResponse();

            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('expires')) {
                obj['expires'] = ApiClient.convertToType(data['expires'], 'Date');
            }
            if (data.hasOwnProperty('minVersions')) {
                obj['minVersions'] = DoAuthResponseMinVersions.constructFromObject(data['minVersions']);
            }
            if (data.hasOwnProperty('disallowedRules')) {
                obj['disallowedRules'] = ApiClient.convertToType(data['disallowedRules'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DoAuthResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DoAuthResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DoAuthResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // validate the optional field `minVersions`
        if (data['minVersions']) { // data not null
          DoAuthResponseMinVersions.validateJSON(data['minVersions']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['disallowedRules'])) {
            throw new Error("Expected the field `disallowedRules` to be an array in the JSON data but got " + data['disallowedRules']);
        }

        return true;
    }


}

DoAuthResponse.RequiredProperties = ["username", "token", "expires", "minVersions", "disallowedRules"];

/**
 * @member {String} username
 */
DoAuthResponse.prototype['username'] = undefined;

/**
 * @member {String} token
 */
DoAuthResponse.prototype['token'] = undefined;

/**
 * @member {Date} expires
 */
DoAuthResponse.prototype['expires'] = undefined;

/**
 * @member {module:model/DoAuthResponseMinVersions} minVersions
 */
DoAuthResponse.prototype['minVersions'] = undefined;

/**
 * @member {Array.<String>} disallowedRules
 */
DoAuthResponse.prototype['disallowedRules'] = undefined;






export default DoAuthResponse;

