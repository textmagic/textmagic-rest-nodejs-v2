/**
 * Textmagic API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateEmailCampaignRequestRecipients model module.
 * @module model/CreateEmailCampaignRequestRecipients
 * @version 3.0.43874
 */
class CreateEmailCampaignRequestRecipients {
    /**
     * Constructs a new <code>CreateEmailCampaignRequestRecipients</code>.
     * Campaign recipients configuration.
     * @alias module:model/CreateEmailCampaignRequestRecipients
     * @param contactIds {Array.<Number>} Array of contact IDs to send to.
     * @param emails {Array.<String>} Array of email addresses to send to.
     * @param groupIds {Array.<Number>} Array of group IDs to send to.
     */
    constructor(contactIds, emails, groupIds) { 
        
        CreateEmailCampaignRequestRecipients.initialize(this, contactIds, emails, groupIds);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, contactIds, emails, groupIds) { 
        obj['contactIds'] = contactIds;
        obj['emails'] = emails;
        obj['groupIds'] = groupIds;
    }

    /**
     * Constructs a <code>CreateEmailCampaignRequestRecipients</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateEmailCampaignRequestRecipients} obj Optional instance to populate.
     * @return {module:model/CreateEmailCampaignRequestRecipients} The populated <code>CreateEmailCampaignRequestRecipients</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateEmailCampaignRequestRecipients();

            if (data.hasOwnProperty('contactIds')) {
                obj['contactIds'] = ApiClient.convertToType(data['contactIds'], ['Number']);
            }
            if (data.hasOwnProperty('emails')) {
                obj['emails'] = ApiClient.convertToType(data['emails'], ['String']);
            }
            if (data.hasOwnProperty('groupIds')) {
                obj['groupIds'] = ApiClient.convertToType(data['groupIds'], ['Number']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateEmailCampaignRequestRecipients</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateEmailCampaignRequestRecipients</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateEmailCampaignRequestRecipients.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['contactIds'])) {
            throw new Error("Expected the field `contactIds` to be an array in the JSON data but got " + data['contactIds']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['emails'])) {
            throw new Error("Expected the field `emails` to be an array in the JSON data but got " + data['emails']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['groupIds'])) {
            throw new Error("Expected the field `groupIds` to be an array in the JSON data but got " + data['groupIds']);
        }

        return true;
    }


}

CreateEmailCampaignRequestRecipients.RequiredProperties = ["contactIds", "emails", "groupIds"];

/**
 * Array of contact IDs to send to.
 * @member {Array.<Number>} contactIds
 */
CreateEmailCampaignRequestRecipients.prototype['contactIds'] = undefined;

/**
 * Array of email addresses to send to.
 * @member {Array.<String>} emails
 */
CreateEmailCampaignRequestRecipients.prototype['emails'] = undefined;

/**
 * Array of group IDs to send to.
 * @member {Array.<Number>} groupIds
 */
CreateEmailCampaignRequestRecipients.prototype['groupIds'] = undefined;






export default CreateEmailCampaignRequestRecipients;

