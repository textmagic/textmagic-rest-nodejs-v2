/**
 * Textmagic API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CustomFieldListItem from './CustomFieldListItem';

/**
 * The CreateContactRequest model module.
 * @module model/CreateContactRequest
 * @version 3.0.43879
 */
class CreateContactRequest {
    /**
     * Constructs a new <code>CreateContactRequest</code>.
     * @alias module:model/CreateContactRequest
     */
    constructor() { 
        
        CreateContactRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CreateContactRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateContactRequest} obj Optional instance to populate.
     * @return {module:model/CreateContactRequest} The populated <code>CreateContactRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateContactRequest();

            if (data.hasOwnProperty('firstName')) {
                obj['firstName'] = ApiClient.convertToType(data['firstName'], 'String');
            }
            if (data.hasOwnProperty('lastName')) {
                obj['lastName'] = ApiClient.convertToType(data['lastName'], 'String');
            }
            if (data.hasOwnProperty('phone')) {
                obj['phone'] = ApiClient.convertToType(data['phone'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('companyName')) {
                obj['companyName'] = ApiClient.convertToType(data['companyName'], 'String');
            }
            if (data.hasOwnProperty('lists')) {
                obj['lists'] = ApiClient.convertToType(data['lists'], 'String');
            }
            if (data.hasOwnProperty('favorited')) {
                obj['favorited'] = ApiClient.convertToType(data['favorited'], 'Boolean');
            }
            if (data.hasOwnProperty('blocked')) {
                obj['blocked'] = ApiClient.convertToType(data['blocked'], 'Boolean');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'Number');
            }
            if (data.hasOwnProperty('customFieldValues')) {
                obj['customFieldValues'] = ApiClient.convertToType(data['customFieldValues'], [CustomFieldListItem]);
            }
            if (data.hasOwnProperty('local')) {
                obj['local'] = ApiClient.convertToType(data['local'], 'Number');
            }
            if (data.hasOwnProperty('country')) {
                obj['country'] = ApiClient.convertToType(data['country'], 'String');
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], ['Number']);
            }
            if (data.hasOwnProperty('owner')) {
                obj['owner'] = ApiClient.convertToType(data['owner'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateContactRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateContactRequest</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['firstName'] && !(typeof data['firstName'] === 'string' || data['firstName'] instanceof String)) {
            throw new Error("Expected the field `firstName` to be a primitive type in the JSON string but got " + data['firstName']);
        }
        // ensure the json data is a string
        if (data['lastName'] && !(typeof data['lastName'] === 'string' || data['lastName'] instanceof String)) {
            throw new Error("Expected the field `lastName` to be a primitive type in the JSON string but got " + data['lastName']);
        }
        // ensure the json data is a string
        if (data['phone'] && !(typeof data['phone'] === 'string' || data['phone'] instanceof String)) {
            throw new Error("Expected the field `phone` to be a primitive type in the JSON string but got " + data['phone']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['companyName'] && !(typeof data['companyName'] === 'string' || data['companyName'] instanceof String)) {
            throw new Error("Expected the field `companyName` to be a primitive type in the JSON string but got " + data['companyName']);
        }
        // ensure the json data is a string
        if (data['lists'] && !(typeof data['lists'] === 'string' || data['lists'] instanceof String)) {
            throw new Error("Expected the field `lists` to be a primitive type in the JSON string but got " + data['lists']);
        }
        if (data['customFieldValues']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['customFieldValues'])) {
                throw new Error("Expected the field `customFieldValues` to be an array in the JSON data but got " + data['customFieldValues']);
            }
            // validate the optional field `customFieldValues` (array)
            for (const item of data['customFieldValues']) {
                CustomFieldListItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['country'] && !(typeof data['country'] === 'string' || data['country'] instanceof String)) {
            throw new Error("Expected the field `country` to be a primitive type in the JSON string but got " + data['country']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tags'])) {
            throw new Error("Expected the field `tags` to be an array in the JSON data but got " + data['tags']);
        }

        return true;
    }


}



/**
 * Contact first name.
 * @member {String} firstName
 */
CreateContactRequest.prototype['firstName'] = undefined;

/**
 * Contact last name.
 * @member {String} lastName
 */
CreateContactRequest.prototype['lastName'] = undefined;

/**
 * Phone number in [E.164 format](https://en.wikipedia.org/wiki/E.164).
 * @member {String} phone
 */
CreateContactRequest.prototype['phone'] = undefined;

/**
 * Contact email address.
 * @member {String} email
 */
CreateContactRequest.prototype['email'] = undefined;

/**
 * Company name.
 * @member {String} companyName
 */
CreateContactRequest.prototype['companyName'] = undefined;

/**
 * Contact [list](https://docs.textmagic.com/#tag/Lists) ID. Each contact must be assigned to at least one list.
 * @member {String} lists
 */
CreateContactRequest.prototype['lists'] = undefined;

/**
 * Is the contact marked as favorite?
 * @member {Boolean} favorited
 */
CreateContactRequest.prototype['favorited'] = undefined;

/**
 * Is the contact blocked for outgoing and incoming messaging?
 * @member {Boolean} blocked
 */
CreateContactRequest.prototype['blocked'] = undefined;

/**
 * Force type of phone. Possible values: 0 is landline; 1 is mobile; default is -1 (auto-detection).
 * @member {Number} type
 */
CreateContactRequest.prototype['type'] = undefined;

/**
 * @member {Array.<module:model/CustomFieldListItem>} customFieldValues
 */
CreateContactRequest.prototype['customFieldValues'] = undefined;

/**
 * Treat phone numbers passed in the request body as local.
 * @member {Number} local
 */
CreateContactRequest.prototype['local'] = undefined;

/**
 * The 2-letter ISO country code for local phone numbers, used when local is  set to true. Default is the account country.
 * @member {String} country
 */
CreateContactRequest.prototype['country'] = undefined;

/**
 * An array of tag IDs that will be assigned to the contact.
 * @member {Array.<Number>} tags
 */
CreateContactRequest.prototype['tags'] = undefined;

/**
 * Contact Owner ID
 * @member {Number} owner
 */
CreateContactRequest.prototype['owner'] = undefined;






export default CreateContactRequest;

