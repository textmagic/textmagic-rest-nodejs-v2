/**
 * Textmagic API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SendPhoneVerificationCodeResponse model module.
 * @module model/SendPhoneVerificationCodeResponse
 * @version 3.0.43879
 */
class SendPhoneVerificationCodeResponse {
    /**
     * Constructs a new <code>SendPhoneVerificationCodeResponse</code>.
     * @alias module:model/SendPhoneVerificationCodeResponse
     * @param verifyId {String} The ID of a verification request. This is required to finish the verification request in the next step.
     * @param price {Number} An amount of credit which will be deducted from your account balance when this verification is successfully completed.
     */
    constructor(verifyId, price) { 
        
        SendPhoneVerificationCodeResponse.initialize(this, verifyId, price);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, verifyId, price) { 
        obj['verifyId'] = verifyId;
        obj['price'] = price;
    }

    /**
     * Constructs a <code>SendPhoneVerificationCodeResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SendPhoneVerificationCodeResponse} obj Optional instance to populate.
     * @return {module:model/SendPhoneVerificationCodeResponse} The populated <code>SendPhoneVerificationCodeResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SendPhoneVerificationCodeResponse();

            if (data.hasOwnProperty('verifyId')) {
                obj['verifyId'] = ApiClient.convertToType(data['verifyId'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SendPhoneVerificationCodeResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SendPhoneVerificationCodeResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SendPhoneVerificationCodeResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['verifyId'] && !(typeof data['verifyId'] === 'string' || data['verifyId'] instanceof String)) {
            throw new Error("Expected the field `verifyId` to be a primitive type in the JSON string but got " + data['verifyId']);
        }

        return true;
    }


}

SendPhoneVerificationCodeResponse.RequiredProperties = ["verifyId", "price"];

/**
 * The ID of a verification request. This is required to finish the verification request in the next step.
 * @member {String} verifyId
 */
SendPhoneVerificationCodeResponse.prototype['verifyId'] = undefined;

/**
 * An amount of credit which will be deducted from your account balance when this verification is successfully completed.
 * @member {Number} price
 */
SendPhoneVerificationCodeResponse.prototype['price'] = undefined;






export default SendPhoneVerificationCodeResponse;

