/**
 * Textmagic API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FileMetadataPreview from './FileMetadataPreview';

/**
 * The FileMetadata model module.
 * @module model/FileMetadata
 * @version 3.0.43874
 */
class FileMetadata {
    /**
     * Constructs a new <code>FileMetadata</code>.
     * @alias module:model/FileMetadata
     * @param type {module:model/FileMetadata.TypeEnum} File metadata type.
     * @param height {Number} 
     * @param width {Number} 
     * @param preview {module:model/FileMetadataPreview} 
     */
    constructor(type, height, width, preview) { 
        
        FileMetadata.initialize(this, type, height, width, preview);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type, height, width, preview) { 
        obj['type'] = type;
        obj['height'] = height;
        obj['width'] = width;
        obj['preview'] = preview;
    }

    /**
     * Constructs a <code>FileMetadata</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FileMetadata} obj Optional instance to populate.
     * @return {module:model/FileMetadata} The populated <code>FileMetadata</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FileMetadata();

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('height')) {
                obj['height'] = ApiClient.convertToType(data['height'], 'Number');
            }
            if (data.hasOwnProperty('width')) {
                obj['width'] = ApiClient.convertToType(data['width'], 'Number');
            }
            if (data.hasOwnProperty('preview')) {
                obj['preview'] = FileMetadataPreview.constructFromObject(data['preview']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FileMetadata</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FileMetadata</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FileMetadata.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // validate the optional field `preview`
        if (data['preview']) { // data not null
          FileMetadataPreview.validateJSON(data['preview']);
        }

        return true;
    }


}

FileMetadata.RequiredProperties = ["type", "height", "width", "preview"];

/**
 * File metadata type.
 * @member {module:model/FileMetadata.TypeEnum} type
 */
FileMetadata.prototype['type'] = undefined;

/**
 * @member {Number} height
 */
FileMetadata.prototype['height'] = undefined;

/**
 * @member {Number} width
 */
FileMetadata.prototype['width'] = undefined;

/**
 * @member {module:model/FileMetadataPreview} preview
 */
FileMetadata.prototype['preview'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
FileMetadata['TypeEnum'] = {

    /**
     * value: "image"
     * @const
     */
    "image": "image",

    /**
     * value: "video"
     * @const
     */
    "video": "video"
};



export default FileMetadata;

