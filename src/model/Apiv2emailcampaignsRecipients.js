/*
 * Textmagic API
 * No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
 *
 * OpenAPI spec version: 2
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.10
 *
 * Do not edit the class manually.
 *
 */

(function(root, factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD. Register as an anonymous module.
    define(['ApiClient'], factory);
  } else if (typeof module === 'object' && module.exports) {
    // CommonJS-like environments that support module.exports, like Node.
    module.exports = factory(require('../ApiClient'));
  } else {
    // Browser globals (root is window)
    if (!root.TextmagicClient) {
      root.TextmagicClient = {};
    }
    root.TextmagicClient.Apiv2emailcampaignsRecipients = factory(root.TextmagicClient.ApiClient);
  }
}(this, function(ApiClient) {
  'use strict';

  /**
   * The Apiv2emailcampaignsRecipients model module.
   * @module model/Apiv2emailcampaignsRecipients
   * @version 2.0.43821
   */

  /**
   * Constructs a new <code>Apiv2emailcampaignsRecipients</code>.
   * Campaign recipients configuration.
   * @alias module:model/Apiv2emailcampaignsRecipients
   * @class
   * @param contactIds {Array.<Number>} Array of contact IDs to send to.
   * @param emails {Array.<String>} Array of email addresses to send to.
   * @param groupIds {Array.<Number>} Array of group IDs to send to.
   */
  var exports = function(contactIds, emails, groupIds) {
    this.contactIds = contactIds;
    this.emails = emails;
    this.groupIds = groupIds;
  };

  /**
   * Constructs a <code>Apiv2emailcampaignsRecipients</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/Apiv2emailcampaignsRecipients} obj Optional instance to populate.
   * @return {module:model/Apiv2emailcampaignsRecipients} The populated <code>Apiv2emailcampaignsRecipients</code> instance.
   */
  exports.constructFromObject = function(data, obj) {
    if (data) {
      obj = obj || new exports();
      if (data.hasOwnProperty('contactIds'))
        obj.contactIds = ApiClient.convertToType(data['contactIds'], ['Number']);
      if (data.hasOwnProperty('emails'))
        obj.emails = ApiClient.convertToType(data['emails'], ['String']);
      if (data.hasOwnProperty('groupIds'))
        obj.groupIds = ApiClient.convertToType(data['groupIds'], ['Number']);
    }
    return obj;
  }

  /**
   * Array of contact IDs to send to.
   * @member {Array.<Number>} contactIds
   */
  exports.prototype.contactIds = undefined;

  /**
   * Array of email addresses to send to.
   * @member {Array.<String>} emails
   */
  exports.prototype.emails = undefined;

  /**
   * Array of group IDs to send to.
   * @member {Array.<Number>} groupIds
   */
  exports.prototype.groupIds = undefined;

  return exports;

}));
