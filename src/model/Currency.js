/**
 * Textmagic API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Currency model module.
 * @module model/Currency
 * @version 3.0.43879
 */
class Currency {
    /**
     * Constructs a new <code>Currency</code>.
     * @alias module:model/Currency
     * @param id {String} The 3-letter ISO currency ID.
     * @param unicodeSymbol {String} Unicode-compatible currency symbol.
     * @param htmlSymbol {String} HTML-compatible currency symbol.
     */
    constructor(id, unicodeSymbol, htmlSymbol) { 
        
        Currency.initialize(this, id, unicodeSymbol, htmlSymbol);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, unicodeSymbol, htmlSymbol) { 
        obj['id'] = id;
        obj['unicodeSymbol'] = unicodeSymbol;
        obj['htmlSymbol'] = htmlSymbol;
    }

    /**
     * Constructs a <code>Currency</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Currency} obj Optional instance to populate.
     * @return {module:model/Currency} The populated <code>Currency</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Currency();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('unicodeSymbol')) {
                obj['unicodeSymbol'] = ApiClient.convertToType(data['unicodeSymbol'], 'String');
            }
            if (data.hasOwnProperty('htmlSymbol')) {
                obj['htmlSymbol'] = ApiClient.convertToType(data['htmlSymbol'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Currency</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Currency</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Currency.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['unicodeSymbol'] && !(typeof data['unicodeSymbol'] === 'string' || data['unicodeSymbol'] instanceof String)) {
            throw new Error("Expected the field `unicodeSymbol` to be a primitive type in the JSON string but got " + data['unicodeSymbol']);
        }
        // ensure the json data is a string
        if (data['htmlSymbol'] && !(typeof data['htmlSymbol'] === 'string' || data['htmlSymbol'] instanceof String)) {
            throw new Error("Expected the field `htmlSymbol` to be a primitive type in the JSON string but got " + data['htmlSymbol']);
        }

        return true;
    }


}

Currency.RequiredProperties = ["id", "unicodeSymbol", "htmlSymbol"];

/**
 * The 3-letter ISO currency ID.
 * @member {String} id
 */
Currency.prototype['id'] = undefined;

/**
 * Unicode-compatible currency symbol.
 * @member {String} unicodeSymbol
 */
Currency.prototype['unicodeSymbol'] = undefined;

/**
 * HTML-compatible currency symbol.
 * @member {String} htmlSymbol
 */
Currency.prototype['htmlSymbol'] = undefined;






export default Currency;

